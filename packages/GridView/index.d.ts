declare function require(string): string;
declare interface GridViewOptions {
  // The grid's rows
  rows?: number;
  // The grid's columns
  columns?: number;
  // The grid's cover
  cover?: string;
  // One block's size
  blockSize?: string;
}
